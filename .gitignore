# Rust (cargo workspace: engine/, server/)
/target/
**/*.rs.bk
Cargo.lock.bak

# Node / frontend (web/)
web/node_modules/
web/dist/
web/.vite/
web/*.tsbuildinfo
web/node_modules/.tmp/

# Editors / OS
.DS_Store
.vscode/
.idea/
*.swp

# See legacy-python/.gitignore for the Python reference implementation's
# own ignore rules (venvs, __pycache__, pytest/mypy/ruff caches, etc.).
